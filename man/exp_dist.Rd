% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist.R
\name{exp_dist}
\alias{exp_dist}
\title{Create an exponential distribution object}
\usage{
exp_dist(rate)
}
\arguments{
\item{rate}{Positive rate parameter \eqn{\lambda} (= 1/mean).}
}
\value{
A list with elements:
\describe{
\item{pdf}{Function: \eqn{f(t) = \lambda e^{-\lambda t}}.}
\item{cdf}{Function: \eqn{F(t) = 1 - e^{-\lambda t}}.}
\item{surv}{Function: \eqn{S(t) = e^{-\lambda t}}.}
\item{name}{Character: \code{"exponential"}.}
\item{params}{Named numeric vector: \code{c(rate = rate)}.}
}
}
\description{
Returns a list representing an exponential distribution with the given
rate parameter. The list contains functions \code{pdf}, \code{cdf}, and \code{surv}
(all vectorized over \code{t}), plus \code{name} and \code{params} fields.
}
\examples{
d <- exp_dist(2)
d$pdf(1)   # 2 * exp(-2)
d$cdf(1)   # 1 - exp(-2)
d$surv(1)  # exp(-2)

}
