% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist.R
\name{make_dists}
\alias{make_dists}
\title{Build distribution objects from a flat parameter vector}
\usage{
make_dists(par, family = "exponential")
}
\arguments{
\item{par}{Numeric parameter vector.}

\item{family}{Character: \code{"exponential"} or \code{"weibull"}.}
}
\value{
List of m distribution objects (each as returned by \code{\link[=exp_dist]{exp_dist()}}
or \code{\link[=weibull_dist]{weibull_dist()}}).
}
\description{
Creates a list of m distribution objects (one per component) from a flat
numeric parameter vector.
}
\details{
For \code{"exponential"}: \code{par} has m elements, one rate per component.
For \code{"weibull"}: \code{par} has 2m elements, interleaved as
\verb{(shape_1, scale_1, shape_2, scale_2, ...)}.
}
\examples{
dists <- make_dists(c(1, 2, 3), family = "exponential")
length(dists)  # 3
dists[[1]]$params  # c(rate = 1)

}
