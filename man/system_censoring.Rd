% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coherent_system.R
\name{system_censoring}
\alias{system_censoring}
\title{Determine per-component censoring status from a coherent system}
\usage{
system_censoring(system, times)
}
\arguments{
\item{system}{A \code{coherent_system} object.}

\item{times}{Numeric vector of component lifetimes (length \code{m}).}
}
\value{
A list with elements \code{T_sys}, \code{critical}, and \code{status}.
}
\description{
Given component lifetimes and a coherent system, identifies:
\describe{
\item{T_sys}{The system lifetime.}
\item{critical}{Index of the critical component (the one in the binding
cut set whose maximum equals \code{T_sys}; ties broken by lowest index).}
\item{status}{Per-component censoring status:
\code{"exact"} if \eqn{T_j = T_{sys}},
\code{"left"} if \eqn{T_j < T_{sys}} (component failed before system),
\code{"right"} if \eqn{T_j > T_{sys}} (component still alive at system
failure).}
}
}
\examples{
sys <- parallel_system(2)
system_censoring(sys, c(1.5, 2.3))
# T_sys = 2.3, critical = 2, status = c("left", "exact")

}
