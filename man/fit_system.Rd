% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/system_density.R
\name{fit_system}
\alias{fit_system}
\title{Fit system MLE via multi-start optimization}
\usage{
fit_system(t_obs, system, family = "exponential", init = NULL, n_starts = 5L)
}
\arguments{
\item{t_obs}{Numeric vector of observed system lifetimes.}

\item{system}{A \link{coherent_system} object.}

\item{family}{Character: \code{"exponential"} or \code{"weibull"}.}

\item{init}{Optional numeric starting values (length = number of
parameters). If \code{NULL}, method-of-moments initialization with spread is
used.}

\item{n_starts}{Integer number of random restarts (default 5).}
}
\value{
A \code{fisher_mle} object (from likelihood.model) with
backward-compatible fields \code{convergence} (integer, 0 = success)
and \code{se} (standard errors). Supports \code{coef()}, \code{vcov()},
\code{logLik()}, \code{AIC()}, \code{confint()}, \code{summary()}.
}
\description{
Maximum likelihood estimation of component lifetime parameters from
system-level failure time observations (Scheme 0). Uses L-BFGS-B as the
primary optimizer with a Nelder-Mead (log-scale) fallback. Standard errors
are computed from the observed Fisher information via \code{numDeriv::hessian}.
}
\examples{
\donttest{
sys <- parallel_system(2)
set.seed(42)
df <- rdata_system(sys, par = c(1, 2), n = 50)
result <- fit_system(df$t, sys)
coef(result)
}

}
\seealso{
\code{\link[=loglik_system]{loglik_system()}} for the log-likelihood,
\code{\link[=rdata_system]{rdata_system()}} for data generation.
}
