% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ie_expand.R
\name{ie_expand}
\alias{ie_expand}
\title{Inclusion-exclusion expansion of a product of CDFs}
\usage{
ie_expand(lam)
}
\arguments{
\item{lam}{Numeric vector of positive rates (one per component).}
}
\value{
A list with elements:
\describe{
\item{sign}{Integer vector of signs (\eqn{+1} or \eqn{-1}).}
\item{rate_sum}{Numeric vector of summed rates for each term.}
}
}
\description{
For a set of exponential rates \code{lam}, computes the inclusion-exclusion
expansion of \eqn{\prod_{i} (1 - e^{-\lambda_i t})}.
}
\details{
Returns sign and rate-sum vectors such that:
\deqn{\prod_i (1 - e^{-\lambda_i t}) = \sum_k \text{sign}_k \cdot
  e^{-\text{rate\_sum}_k \cdot t}}

The number of terms is \eqn{2^m} where \eqn{m} is \code{length(lam)}.
}
\examples{
ie <- ie_expand(c(1, 2))
# Product: (1 - exp(-t)) * (1 - exp(-2t))
# = 1 - exp(-t) - exp(-2t) + exp(-3t)
ie$sign      # c(1, -1, -1, 1)
ie$rate_sum  # c(0, 1, 2, 3)

}
