% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exp_kofn.R
\name{score.exp_kofn}
\alias{score.exp_kofn}
\title{Score function for exponential k-out-of-n model}
\usage{
\method{score}{exp_kofn}(model, ...)
}
\arguments{
\item{model}{An \code{exp_kofn} object created by \code{\link[=kofn]{kofn()}}.}

\item{...}{Additional arguments (ignored).}
}
\value{
A function \verb{function(df, par)} returning a numeric vector of
length \code{m} (the score vector).
}
\description{
Returns a closure \verb{function(df, par)} that computes the gradient of
the log-likelihood with respect to the rate parameters.
}
\details{
Uses numerical differentiation via \code{\link[numDeriv:grad]{numDeriv::grad()}} applied to the
log-likelihood closure.
}
\examples{
model <- kofn(k = 2, m = 2, component = dfr_exponential())
sc <- score(model)
set.seed(1)
df <- rdata(model)(c(1, 2), n = 30)
sc(df, c(1, 2))  # gradient at true parameters

}
