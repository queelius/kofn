% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/system_density.R
\name{loglik_system}
\alias{loglik_system}
\title{Log-likelihood for Scheme 0 (system-level observations only)}
\usage{
loglik_system(t_obs, system, par, family = "exponential")
}
\arguments{
\item{t_obs}{Numeric vector of observed system lifetimes.}

\item{system}{A \link{coherent_system} object.}

\item{par}{Numeric parameter vector (rates for \code{"exponential"}, or
interleaved shape/scale for \code{"weibull"}).}

\item{family}{Character: \code{"exponential"} or \code{"weibull"}.}
}
\value{
Scalar log-likelihood value, or \code{-Inf} if parameters are invalid.
}
\description{
Computes the log-likelihood \eqn{\ell(\theta) = \sum_i \log f_{sys}(t_i;
\theta)} for observed system lifetimes under Scheme 0, where only the
system failure time is observed.
}
\examples{
sys <- parallel_system(2)
set.seed(1)
df <- rdata_system(sys, par = c(1, 2), n = 30)
loglik_system(df$t, sys, par = c(1, 2))

}
\seealso{
\code{\link[=fit_system]{fit_system()}} for MLE, \code{\link[=rdata_system]{rdata_system()}} for data generation.
}
