% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wei_kofn.R
\name{loglik.wei_kofn}
\alias{loglik.wei_kofn}
\title{Log-likelihood for Weibull k-out-of-n system}
\usage{
\method{loglik}{wei_kofn}(model, ...)
}
\arguments{
\item{model}{A \code{wei_kofn} object.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A function \code{function(df, par)} returning a scalar log-likelihood.
}
\description{
Returns a closure \code{function(df, par)} that computes the log-likelihood
for a Weibull k-out-of-n system given data and parameters.
}
\details{
For parallel systems (k = 1), uses the direct Weibull system density
\eqn{f_{sys}(t) = \sum_j f_j(t) \prod_{i \ne j} F_i(t)}.
Supports \code{"exact"} and \code{"right"} observation types.
Left and interval censoring are not supported for Weibull (no IE
expansion); use the exponential model or Scheme 1 for those cases.

For general k, delegates to \code{\link[=loglik_system]{loglik_system()}} with \code{family = "weibull"}.
}
