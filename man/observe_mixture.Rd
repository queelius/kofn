% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/observe.R
\name{observe_mixture}
\alias{observe_mixture}
\title{Mixture of observation schemes}
\usage{
observe_mixture(..., weights = NULL)
}
\arguments{
\item{...}{Observation functors (created by \code{observe_*} functions).}

\item{weights}{Mixing probabilities (numeric vector). If \code{NULL},
uniform weights are used. Weights are normalized to sum to 1.}
}
\value{
Observation functor: \code{function(t_true)} that randomly
selects a constituent scheme and returns its output.
}
\description{
Creates an observation functor that randomly selects from a set of
observation schemes for each observation. This models heterogeneous
monitoring environments where different units may be observed under
different protocols.
}
\examples{
obs <- observe_mixture(
  observe_right_censor(tau = 100),
  observe_periodic(delta = 10, tau = 100),
  weights = c(0.7, 0.3)
)
set.seed(42)
obs(30)
}
