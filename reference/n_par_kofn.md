# Expected parameter count for a homogeneous kofn system.

Expected parameter count for a homogeneous kofn system.

## Usage

``` r
n_par_kofn(m, component)
```

## Arguments

- m:

  Number of components.

- component:

  A `dfr_dist` prototype.

## Value

Integer, the total parameter count for the flat `par` vector.
